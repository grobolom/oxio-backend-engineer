/**
 * E164 is a simple format with a lot of complexity. In the strictest sense,
 * a valid E164 phone # will contain anywhere between 1 and 15 digits, and it
 * must 

const E164Regex = /\+?(\d{3})\s?(\d{3})\s?(\d{7})/;

const extractPhoneNumber = (str) => {

};

module.exports = extractPhoneNumber;
